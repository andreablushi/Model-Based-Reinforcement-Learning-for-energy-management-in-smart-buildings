defaults:
  - algorithm: m2ac
  - dynamics_model: gaussian_mlp_ensemble
  - overrides: m2ac_citylearn
  - _self_

seed: 42
# can use cuda:0 here if GPU available, else cpu
device: "cuda:0"

log_frequency_agent: 100
save_video: false
debug_mode: false
